[phases.setup]
nixPkgs = ["nodejs_22", "gcc", "gnumake", "python3", "pkg-config"]

[phases.build]
cmds = [
  "npm install --include=dev",
  "npm_config_build_from_source=true npm rebuild better-sqlite3",
  "npm run build"
]

[start]
cmd = "NODE_ENV=production node apps/api/dist/index.js"